description: >
  Default executor

docker:
  - image: 'cimg/base:<<parameters.tag>>'

parameters:
  tag:
    # renovate: datasource=docker depName=cimg/base
    default: "2026.04"
    description: >
      Pick a specific cimg/base image variant:
      https://hub.docker.com/r/cimg/base/tags
    type: string
